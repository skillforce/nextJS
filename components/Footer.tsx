import Heading from "./Heading";
import {FC} from 'react';

const Footer:FC = () => {
    return (
        <footer>
            <Heading tag={'div'} text={'footer'} />
        </footer>
    );
};

export default Footer;